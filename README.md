# TURK TELEKOM ZAPRET CONFIG 2025-08-01
This repository contains the configuration files for bypassing the Turk Telekom internet censorship.

## Usage
To use this configuration, you need to have a compatible DNS resolver that supports DNS over HTTPS (DoH) or DNS over TLS (DoT).
You can use the dnscrypt-proxy or any other compatible DNS resolver.

After installing the zapret, copy the this configuration file to the your zapret configuration directory.
